<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Финансовый контроль</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 16px;
      background: #f5f5f5;
    }
    h1 { font-size: 22px; }
    .card {
      background: #fff;
      padding: 16px;
      border-radius: 10px;
      margin-bottom: 16px;
    }
    input, button {
      width: 100%;
      padding: 10px;
      margin-top: 8px;
      font-size: 16px;
    }
    button {
      background: #4CAF50;
      color: white;
      border: none;
      border-radius: 6px;
    }
    .result { margin-top: 10px; }
  </style>
</head>
<body>

<h1>Сегодня</h1>

<div class="card">
  <label>Часы работы</label>
  <input type="number" id="hours" placeholder="Например, 6">

  <label>Заработал сегодня (₽)</label>
  <input type="number" id="income" placeholder="Например, 3500">

  <button onclick="saveDay()">Сохранить день</button>
</div>

<div class="card">
  <h2>Результат</h2>
  <div class="result" id="output"></div>
</div>

<script>
  const LIFE_MONTH = 25000;
  const DEBT_MONTH = 15000;
  const DAYS = 30;
  const SAVE_PERCENT = 0.3;

  function saveDay() {
    const hours = Number(document.getElementById('hours').value);
    const income = Number(document.getElementById('income').value);

    if (!income) return alert("Введите доход");

    const lifeDay = LIFE_MONTH / DAYS;
    const debtDay = DEBT_MONTH / DAYS;

    const rest = income - lifeDay - debtDay;
    const savings = rest > 0 ? Math.round(rest * SAVE_PERCENT) : 0;
    const free = rest > 0 ? Math.round(rest * (1 - SAVE_PERCENT)) : 0;

    const text = `
      💰 Заработал: ${income} ₽<br>
      🏠 На жизнь: ${Math.round(lifeDay)} ₽<br>
      💳 Долги: ${Math.round(debtDay)} ₽<br>
      📈 Можно отложить: <b>${savings} ₽</b><br>
      🟢 Свободно: ${free} ₽
    `;

    document.getElementById('output').innerHTML = text;
  }
</script>

</body>
</html>